# XIAO ESP32-S3 Sense, 8 MB flash. An iroh node is ~4 MB, so the stock
# "single app, large" table (1.5 MiB factory) cannot hold it; this one gives the
# app 6 MiB and keeps the rest for PHY, the owner's NVS and the device key.
#
# `identity` holds the device key and nothing an owner writes, and it ENDS the
# flash so a flasher can stop before it. That separation is not decoration: an
# ESP32-CAM re-minted its DID on every `nvs` rewrite until the key was moved
# out of the owner's partition (espino ledger, 2026-09-05). This table used to
# keep the key in `nvs` and said so in this comment; it was written before that
# was understood.
#
# Flash with: espflash flash --partition-table partitions.csv --monitor
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x600000,
identity, data, nvs,     0x7fd000, 0x3000,
